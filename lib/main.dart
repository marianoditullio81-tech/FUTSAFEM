import 'package:flutter/material.dart';

import 'app.dart';

// La inicialización de Firebase (firebase_core.initializeApp con
// firebase_options.dart generado por FlutterFire CLI) se agrega cuando el
// proyecto Firebase exista (Fase 0 del plan de desarrollo).
void main() {
  runApp(const FutsafemApp());
}
